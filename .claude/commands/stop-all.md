# Stop All Agents

Stop all running agents managed by Agent Monitor.

## Command

```bash
curl -s -X POST http://localhost:3456/api/agents/actions/stop-all -H 'Content-Type: application/json'
echo "All agents stopped."
```
